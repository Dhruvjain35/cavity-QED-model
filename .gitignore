# dependencies / build output
node_modules/
dist/
*.log
.DS_Store

# Rust / wasm build artifacts
wasm/target/
wasm/pkg/

# Python golden-generator venv (the committed goldens under wasm/golden/ are kept)
golden/.venv/
__pycache__/
*.pyc

# internal scratch (workflow scripts, design notes)
.wf-*.js
.impeccable.md

# screenshot tool output (npm run screenshot) + throwaway verification scripts
screenshots/
scratch/

# WASM web build committed so Vercel/any clone can build without a Rust toolchain
!wasm/pkg-web/
!wasm/pkg-web/**
.vercel

# throwaway verification scripts
.ver/

# temp workflow scripts
.wf/

# internal planning/build/evidence docs — kept locally, never in the public repo
_internal/
